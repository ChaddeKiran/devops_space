apiVersion: v1
kind: Service
metadata:
  name: bookmycar-backend
  namespace: prod
spec:
  selector:
    app: bookmycar-backend
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
